import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog",
};

const Blog = () => {
  return (
    <div className="demo">
      <h1>Blogs</h1>
    </div>
  );
};

export default Blog;
