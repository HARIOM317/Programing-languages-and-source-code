// Template literals is also called Template Strings
let num = 12;

console.log(`Table of ${num} using Template literals - \n`);

for(let i=1; i<=10; i++){
    // Template string
    console.log(`${num} * ${i} = ${num*i}`);
}