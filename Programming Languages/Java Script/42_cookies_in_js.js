// Cookies are key-value pair used to store data in browser
// It will work on only browser
console.log(document.cookie);

// adding cookies
document.cookie = "name=hariomsinghrajput";
document.cookie = "address=bhopal";

console.log(document.cookie);