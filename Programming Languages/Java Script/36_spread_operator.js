// ...arrayName - it is called spread operator and used to add all elements of an array into another array

const color = ['red', 'blue', 'purple', 'pink', 'brown', 'green', 'orange'];

const allColors = [...color, 'black', 'white', 'yellow'];

console.log(allColors);